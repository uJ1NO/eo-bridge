# Core (always needed)
pyrtl>=0.11.0
numpy>=1.24
matplotlib>=3.7

# Photonic backend (optional - the analytical MZM works without it)
# Pulls in jax/sax/etc.  ~300 MB once installed.
simphony>=0.7.0 ; python_version >= "3.10"
